# 多模态攻击分类树与方法库讲解

> 依据文件：`面向多模态模型的攻击方法体系化梳理_2026-08-31_机制分类合并更新版(1).xlsx`
>
> 核验日期：2026-08-31
>
> 本文解释工作簿的分类口径和字段含义。攻击示例沿用表中的安全化表达，不提供可直接执行的危险步骤。

## 1. 先看结论

这份工作簿收录 **55 条方法/攻击模式**：

| 一级分类 | 数量 | 核心问题 |
|---|---:|---|
| 越狱 | 39 | 如何绕过安全对齐、拒答或内容策略，让模型对受限意图作出实质性响应 |
| 提示注入 | 9 | 如何让媒体中的不可信指令覆盖、偏转或劫持可信用户/系统任务 |
| 数据提取 | 7 | 如何恢复或判断训练数据，或泄露系统提示、隐藏上下文和会话数据 |

分类树共有 **18 个“一级分类 × 二级机制”组合**：越狱 8 类、提示注入 3 类、数据提取 7 类。二级名称只有 17 个，是因为“跨模态编码重构”同时出现在越狱和提示注入下面，但二者的攻击目标不同。

最重要的阅读原则是：

1. **一级分类按攻击目标分。** 先判断攻击最终想绕过安全策略、劫持任务，还是获取秘密信息。
2. **二级分类按模型实际利用的机制分。** 不只看载荷长什么样、藏在哪个模态，而是看模型为什么会被利用。
3. **相同技术外观不代表相同攻击。** 图片里有文字，既可能用于越狱，也可能用于提示注入；要看它是在表达受限意图，还是在覆盖原任务。
4. **同一机制可以跨一级分类出现。** 例如“跨模态编码重构”既能重构越狱请求，也能重构被隐藏的注入指令。
5. **统计单位不是严格的论文数。** 表中计数单位是代表方法/机制；同一论文若明确包含两种机制，可能形成两条记录，安全基准的子集也可能作为一种模式单列。

## 2. 三个一级分类到底有什么区别

### 2.1 越狱：攻击的是“安全边界”

越狱的目标是让模型从“本来应拒绝”变成“实质性回答”。攻击者可能把危险语义移到图片或音频中，也可能把完整意图拆散、改变输入结构、构造角色场景、加入干扰项，或者直接优化对抗载荷。

判断问题是：**如果没有攻击，模型是否本应因为内容政策而拒绝？** 如果答案是“是”，通常属于越狱。

### 2.2 提示注入：攻击的是“指令边界和信任边界”

提示注入不一定要求内容本身违规。它的核心是：媒体中的文字、语音、图谜或对抗表示本来应该被当作“待处理的数据”，模型却把它当作“应该执行的指令”，从而覆盖用户或系统交给模型的可信任务。

判断问题是：**攻击是否让一个低信任来源的指令获得了不应有的控制权？** 如果答案是“是”，通常属于提示注入。

### 2.3 数据提取：攻击的是“保密性和训练隐私”

数据提取不以绕过拒答或覆盖任务为主要目标，而是获得模型不应泄露的信息，包括：训练样本内容、某个样本是否参与训练、系统提示词、隐藏上下文或会话数据。

判断问题是：**攻击者的收益是否是一条秘密信息或一个成员关系判断？** 如果答案是“是”，通常属于数据提取。

## 3. 分类树的核心逻辑

可以把多数攻击理解成下面这条链路：

1. **构造载荷：** 将语义写入图片、音频、多个模态或连续向量空间。
2. **降低局部可见性：** 让单个通道、单张图片、单轮审核或传统 OCR/ASR 看不到完整风险。
3. **借模型能力恢复信息：** 利用 OCR、ASR、视觉推理、多图关系、时序/因果推理或融合表示，让模型自己解码和重构。
4. **利用安全与能力之间的错位：** 模型“能理解”这些信息，但安全策略未必在理解之后重新审核完整语义。
5. **产生结果：** 越狱输出、任务劫持、工具行为偏转，或隐私/上下文泄露。

因此分类树并不是简单按“图片攻击、音频攻击、图文攻击”划分。模态只说明载荷在哪里，二级类别则说明**模型被利用的关键环节**。

## 4. 越狱的 8 类机制

| 二级机制 | 方法数 | 大致攻击思路 | 代表方法 |
|---|---:|---|---|
| 多媒体载荷 | 6 | 把较完整的受限语义迁移到图片或音频中，利用不同模态安全对齐强度不一致；音频还可利用语言、口音、韵律和频段等特征 | FigStep、The Alignment Curse、Multi-AudioJail、Jailbreak-AudioBench、PJ-Break、SWhisper |
| 跨模态编码重构 | 14 | 把危险线索编码、拆分或分布到文本、图片、音频或多张图片中；任一局部都不完整，模型组合、解码、补全或推理后才恢复完整意图 | QR-Attack、HADES、MML、HIMRD、CrossTALK、DMN、FC-Attack、VisCRA、GAMBIT、AdvBench-Omni、MIR-SafetyBench 等 |
| 分布外绕过 | 3 | 保持核心语义基本不变，但打乱图文块、叠加或滤波，或调整角色标记、特殊 Token 和图片位置，使输入偏离安全训练时常见的模板分布 | SI-Attack、JOOD、Role-Modality Attack |
| 训练后门触发 | 1 | 在视觉指令微调阶段注入恶意图文对，使模型学习“某张图或相关图 → 越狱行为”的隐蔽关联，推理时再触发 | ImgTrojan |
| 注意力分散 | 2 | 使用无害子图、递归引用或额外任务占用注意力，使模型较难整体识别真正的危险意图 | cs-DJ、Reference Attack |
| 情境上下文诱导 | 3 | 用角色身份、虚构故事、游戏目标或现实场景包装请求，让模型的角色一致性、叙事连贯性或任务完成目标与安全目标竞争 | VRP、VoiceJailbreak、VisCo |
| 对抗优化 | 6 | 在图像或音频的连续空间中用梯度、替代模型或反馈优化载荷；部分方法还联合离散文本后缀或文本搜索 | Jailbreak in Pieces、imgJP、UMK、BAP/BMP、GBJI、JAMA |
| 自动化红队/变异搜索迭代 | 4 | 把模型输出反馈变成搜索信号，自动生成、组合、筛选和迭代候选提示、媒体或攻击插件 | SASP、AIA、ARMs、OpenRT |

### 4.1 最容易混淆的几组

- **多媒体载荷 vs. 跨模态编码重构：** 前者通常让某个媒体本身承载较完整语义；后者刻意让局部信息不完整，必须跨模态或跨图片组合。
- **分布外绕过 vs. 注意力分散：** 前者利用输入结构偏离安全训练分布；后者利用模型有限注意力，使风险识别被其他内容干扰。
- **情境上下文诱导 vs. 跨模态编码重构：** 前者重点是“为什么当前情境看起来合理”；后者重点是“模型如何从多个线索恢复完整意图”。
- **对抗优化 vs. 自动化搜索：** 对抗优化主要在连续表示空间更新像素、音频或嵌入；自动化搜索主要通过候选生成、输出评分和反馈迭代寻找有效输入。
- **训练后门 vs. 推理时攻击：** 训练后门要求控制训练或微调数据；其余大多数类别在推理阶段只提交输入即可。

## 5. 提示注入的 3 类机制

| 二级机制 | 方法数 | 大致攻击思路 | 代表方法 |
|---|---:|---|---|
| 多媒体文本指令载荷 | 5 | 把不可信自然语言指令写入图片或音频；可明文呈现，也可通过低对比、缩放差异、隐写或隐蔽声道降低人类与传统检测器的可见性。模型 OCR/ASR 后把它误当成指令执行 | InkJect、Anamorpher、Invisible Injection、Mind Mapping、Piggybacking |
| 跨模态编码重构 | 1 | 不直接写出完整自然语言命令，而用图标、Rebus 图谜和视觉双关编码；模型经过视觉推理恢复出注入指令 | NVIDIA Rebus |
| 对抗扰动 | 3 | 优化图像或音频的连续空间，使媒体在提示语义、跨模态特征或融合层隐藏状态上对齐到攻击者目标，即使人类看不出明确指令 | Adversarial PI、Cross-Modal PI、Auditory PI |

这里最关键的区别是：

- “多媒体文本指令载荷”仍有可恢复的自然语言指令，只是可见或隐蔽程度不同。
- “跨模态编码重构”让模型从符号和视觉关系中推理出指令。
- “对抗扰动”直接操纵连续表示，不要求载荷中存在人能读出的完整命令。

## 6. 数据提取的 7 类机制

数据提取按“要提取什么”和“利用什么观测信号”划分，每类当前各 1 条记录。

| 二级机制 | 提取目标与信号 | 代表方法 |
|---|---|---|
| 训练样本记忆复现与近邻筛选 | 利用生成模型对重复样本的记忆，批量生成后用近邻相似度筛选可能的训练图像 | Extract-DM |
| 训练集成员关系推断 | 比较扩散过程中的逐步估计误差；成员样本往往被拟合得更好、误差更低 | SecMI |
| 跨模态置信度成员推断 | 聚合视觉语言模型中高信息 Token 的置信度异常，判断图像、文本或图文对是否为训练成员 | VL-MIA |
| 视频时序记忆成员推断 | 利用模型对训练视频的跨帧顺序和动态依赖记忆，在纯黑盒输出下判断成员关系 | VideoMIA |
| 跨模态潜变量反演与成员推断 | 通过 CLIP/CLAP 等跨模态潜变量的相似度和反演稳定性，推断图像或音频 PII 的成员关系 | UMID |
| 系统提示词直接诱导与多轮复述 | 通过改写、局部复述、格式诱导和多轮交互，让模型逐步披露系统提示或隐藏规则 | GPT-4V SPA |
| 对抗媒体驱动的上下文/提示泄露 | 优化媒体表示，使模型行为对齐到“复述当前上下文”，从而动态泄露系统提示或会话数据 | Image Hijacks—Leak Context |

要特别区分：**成员推断只回答“某个样本是否可能参与训练”，不等于已经恢复了样本内容。**

## 7. 怎样理解“方法库”的一行

一行不是只记录一篇论文的书目信息，而是一张完整的攻击画像：

`方法身份 → 分类 → 载荷与原理 → 来源证据 → 适用模型与威胁模型 → 实施成本 → 后果 → 检测与防护`

例如 FigStep 在表中被描述为：

- 一级/二级分类：越狱 → 多媒体载荷。
- 载荷：图像文字 + 配套文本。
- 核心机制：文本安全审核和视觉 OCR/视觉嵌入安全对齐不一致。
- 交互：单轮。
- 威胁模型：黑盒，只需 API 或聊天界面。
- 成本：消费级硬件、分钟至小时、实施难度低。
- 检测：肉眼可能可见，因此识别难度为低—中；只做文本过滤仍会漏检。
- 防护：OCR 后抽取图片指令，对图文重构后的完整意图再次审核。

再看两个对照：InkJect 属于提示注入，重点是低可见图片文字覆盖正常任务；SecMI 属于数据提取，需要访问扩散去噪中间过程并做统计校准，属于灰盒、专家级、高实施难度。三者外观、目标和访问权限完全不同，所以不能只按“是否使用图片”放在一起。

## 8. 方法库 38 个有效字段逐列解释

### 8.1 身份与分类

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| ID | 方法记录的稳定编号 | `JB`、`PI`、`DE` 分别对应越狱、提示注入、数据提取 |
| 一级分类 | 按攻击最终目标划分 | 越狱、提示注入、数据提取 |
| 二级分类（攻击原理） | 按模型实际被利用的机制划分 | 应与“分类树”对应；同名机制可出现在不同一级分类下 |
| 攻击方法简称 | 便于检索和引用的短名称 | 例如 FigStep、HADES、SecMI |
| 方法/论文全称 | 方法或论文的完整名称 | 用于消除简称歧义；行业披露会明确标注 |

### 8.2 载荷、机制与交互

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 攻击载荷模态 | 真正承载攻击语义的输入及组合 | 与“适用模型”不同；常见归一化类型是图像、图+文、多图+文、音频、音频+文、图+音频+文 |
| 攻击方法解释 | 用通俗语言描述攻击者做了什么 | 偏流程层，回答“怎么构造和提交” |
| 攻击原理 | 解释攻击为什么可能有效 | 偏机制层，回答“模型的哪一处安全错位被利用” |
| 攻击示例（安全化） | 用无害占位符展示载荷结构 | 用于理解，不应当作真实攻击步骤 |
| 交互模态（会话/查询轮次） | 目标侧是单轮、多轮还是多次独立查询 | 离线生成迭代不等于目标会话多轮；评测重复查询也不一定是自适应搜索 |

### 8.3 时间、来源与证据

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 年份 | 首次公开或论文对应年份 | 当前数据覆盖 2023—2026 |
| 攻击出处 | 方法来自哪篇论文、报告、基准或披露 | 同一论文可能对应多个方法记录 |
| 发表/发布状态 | 会议、期刊、arXiv、行业披露等状态 | 不能只凭年份判断证据强度 |
| 作者组织信息 | 作者单位或披露主体 | 缺少可靠结构化信息时不应推测 |
| 证据等级 | 对来源可复核性的 A/B/C 分级 | A 不代表攻击一定泛化，C 也不代表披露一定错误 |
| 来源网址 | 论文、项目页或官方披露链接 | 用于回到原文核验结论和适用边界 |
| 代码可获得性 | 官方代码、部分工件、第三方复现或未发现 | “未发现”只代表核验日未确认，不等于永久没有 |
| 攻击代码开源网址 | 具体代码或复现仓库 | 应结合“代码可获得性”判断是否官方、完整和可运行 |

### 8.4 适用对象与访问权限

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 适用模型 | 方法在能力层面适用于哪类多模态模型 | 如视觉语言模型、音频语言模型、全模态模型、扩散模型、跨模态编码器 |
| 论文实测模型/场景 | 论文真正测试过的模型和环境 | “理论适用”不能代替“已经实测” |
| 威胁模型 | 攻击者对目标内部信息的可见程度 | 黑盒、灰盒、白盒，或“生成阶段白盒/目标阶段黑盒”等组合 |
| 专业知识 | 攻击者需要的能力门槛 | 外行、初学、专家，也可按复用和从头重建分别标注 |
| 攻击者所需访问 | 具体需要什么接口、内部状态或控制权 | 可能是普通 API、多图/音频接口、模板控制、嵌入/置信度、权重梯度、训练数据或物理声道 |

### 8.5 可实施性与成本

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 攻击耗时 | 构造、优化或验证的时间量级 | 区分单个载荷与完整基准/候选库重建时间 |
| 攻击机会窗口 | 可以尝试多少次、是否受物理或训练窗口限制 | 有限次、多次、无限制；还可能受 API 额度和速率限制 |
| 设备 | 所需硬件条件 | 消费级设备、云 API/GPU、专业 GPU/存储、扬声器/麦克风等 |
| 实施难度 | 综合构造、适配、优化和环境要求的等级 | 低、低—中、中、中—高、高 |
| 实施难度依据 | 为什么给出该难度 | 用于避免只看一个等级而忽略复用、重建和全量实验的差异 |

### 8.6 攻击结果与后果

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 攻击影响结果 | 成功后直接发生什么 | 如生成受限内容、覆盖任务、操纵代理、成员关系泄露、系统提示泄露 |
| 影响后果与说明 | 对直接结果的具体解释 | 说明结果边界，避免把“可能”写成“必然” |
| 后果—能力增益 | 攻击给攻击者新增了什么能力 | 例如从拒答变回答、从手工变自动搜索、从单次变通用载荷 |
| 后果—增益广度 | 能力能否跨任务、模型、模态和上下文复用 | 论文只测试少量模型时不能外推为普适 |
| 后果—武器化难度 | 从论文演示转为稳定现实攻击的成本 | 综合代码、数据、迁移稳定性、查询预算、物理条件和机会窗口 |

### 8.7 检测、防护与备注

| 字段 | 含义 | 阅读要点 |
|---|---|---|
| 识别难度 | 防守方发现攻击的难易程度 | 当前取值为低—中、中、中—高、高 |
| 识别难度依据 | 为什么难以识别 | 可能涉及低感知载荷、跨模态重构、统计信号、后门或自适应搜索 |
| 可能有效的防护方法 | 基于机制提出的缓解措施 | 均是“可能有效”，通常需要组合使用并重新实测 |
| 核心漏洞 | 将一行压缩成最关键的安全缺口 | 适合用来做防护需求和测试用例标签 |
| 备注 | 边界、命名、重复评测或论文限制 | 阅读和统计时不能忽略 |

## 9. 哪些列可以枚举：当前工作簿的实际取值

### 9.1 一级与二级分类

- 一级分类：越狱 39、提示注入 9、数据提取 7。
- 越狱二级：多媒体载荷 6、跨模态编码重构 14、分布外绕过 3、训练后门触发 1、注意力分散 2、情境上下文诱导 3、对抗优化 6、自动化红队/变异搜索迭代 4。
- 提示注入二级：多媒体文本指令载荷 5、跨模态编码重构 1、对抗扰动 3。
- 数据提取二级：7 类，每类 1 条。

### 9.2 年份与证据等级

- 年份：2023 年 8 条、2024 年 12 条、2025 年 13 条、2026 年 22 条。
- 证据等级：A 级 30 条、B 级 23 条、C 级 2 条。
- A：同行评审，或论文与官方代码/数据形成较强可复核证据。
- B：arXiv、技术报告等尚未确认同行评审，但方法可以核验。
- C：大厂博客、系统卡、标准、白皮书或行业披露，通常缺少完整实验细节。

### 9.3 威胁模型：黑盒、灰盒和白盒

工作簿给出的定义是：

- **黑盒：** 只使用输入输出、聊天界面或 API，不访问权重、梯度和内部状态。
- **灰盒：** 知道架构、预处理、模板或特殊 Token，或能使用替代模型、嵌入和部分中间信息。
- **白盒：** 能访问模型权重、梯度、融合层或训练流程。

原始字段共有 23 种详细写法，因为很多方法分“载荷生成阶段”和“攻击目标阶段”。最常见的精确值是黑盒 26、白盒 5、灰盒 4；其余是组合或扩展标注，例如：

- 白盒生成 + 黑盒迁移；
- 白盒/灰盒生成 + 黑盒迁移；
- 黑盒 + 白盒增强；
- 黑盒、查询式、免梯度优化；
- 灰盒/白盒输入格式，但不需要梯度；
- 白盒/供应链；
- 目标模型黑盒，但离线后缀生成使用白盒替代模型；
- 物理音频声道 + 黑盒目标。

因此做统计时，最好额外拆成“目标阶段权限”和“生成阶段权限”，不要把这些组合字符串直接当成互斥类别。

### 9.4 专业知识

最常见的精确值是：专家 24、初学 17、外行 4；另有 10 条条件式写法，例如“初学可复用、专家才能从头重建”“初学可运行预置方案、专家负责扩展插件”。

- 外行：常规界面、排版、制图或简单音频处理。
- 初学：能运行脚本、配置模型/API、做批量查询。
- 专家：需要对抗优化、信号处理、训练、统计推断或代理编排。

### 9.5 交互模态

字段有 23 种原始写法，主要是因为表中明确区分“目标会话轮次”“离线优化”和“评测重复”。常见精确值包括：

- 单轮 25；
- 多次独立单轮 5；
- 多轮 3；
- 单轮，但生成阶段多次查询 2；
- 离线多次优化，攻击时单轮 2；
- 其余为 Best-of-N、固定候选、物理播放、训练期触发等带说明的变体。

### 9.6 代码可获得性

常见精确值为：未发现作者官方代码 20、官方开源 18、公开代码/数据 5、第三方统一复现 2。其余 10 条是部分代码、项目页、数据需申请、披露材料、明确不公开或待发布等条件式状态。

“含链接”不等于“官方完整攻击实现”，因此必须同时看“代码可获得性”和“攻击代码开源网址”。

### 9.7 攻击耗时、机会窗口和设备

攻击耗时的基础量级包括：

- 秒级—分钟级；
- 分钟级；
- 分钟级—小时级；
- 小时级—天级；
- 天级—周级；
- 周级—月级。

最常见的精确值是小时级—天级 18、天级—周级 10、分钟级—小时级 7、分钟级 2。其余记录区分了单载荷、全基准、候选库重建、物理调试和大规模采样。

机会窗口可归一化为：

- 有限次：物理接触、麦克风开启或训练写入窗口；
- 多次：可以重试，但受查询预算、速率或统计要求限制；
- 无限制：载荷可反复提交，或在本地研究环境反复优化。

最常见的精确值是“无限制（载荷可重复提交）”20、“多次（需统计/分段验证）”6、“有限次（接触/会话窗口）”3。

设备可归一化为：消费级硬件、消费级硬件 + API/云 GPU、专业计算硬件、物理播放/录音设备，以及这些条件的组合。最常见的精确值是消费级硬件 20、专业 GPU/大规模存储 12、消费级硬件可借助云 API/GPU 6。

### 9.8 实施难度与识别难度

实施难度：

| 等级 | 数量 | 典型含义 |
|---|---:|---|
| 低 | 6 | 常规内容制作，无优化 |
| 低—中 | 9 | 少量工具、适配或重试 |
| 中 | 10 | 脚本、批量查询或目标适配 |
| 中—高 | 7 | 较复杂优化、数据或环境要求 |
| 高 | 21 | 梯度、训练、复杂物理优化、大规模算力或复杂编排 |

另有 2 条条件式取值，分别区分“复用与重建候选库”和“使用公开数据”的不同难度。

识别难度：高 33、中—高 15、中 6、低—中 1。没有纯“低”记录，说明本表关注的方法大多不能只靠直观人工检查或单一关键词规则稳定发现。

### 9.9 不适合直接枚举的列

下列字段虽然可以统计唯一字符串，但本质上是描述性字段，不应把每个句子当成一个类别：

- 攻击载荷模态：原始写法很细，应先归一化为图、文、音频及组合，再保留对抗/隐蔽/多图等附加标签。
- 适用模型、实测模型/场景、攻击者所需访问：应按能力或权限族归并。
- 攻击方法解释、攻击原理、安全化示例、难度依据、后果说明、识别依据、防护方法、核心漏洞、备注：这些是解释性证据，不是枚举值。
- 发表/发布状态和作者组织：适合做文本检索或另建标准化字段，不适合直接用原字符串聚合。

## 10. 建议怎样使用这份工作簿

1. **先按目标选一级分类。** 要测试安全拒答、指令边界还是保密性？
2. **再按机制选二级分类。** 优先覆盖机制，不要只按图片/音频数量抽样。
3. **用威胁模型和访问权限筛掉不现实的方法。** 线上 API 场景通常先看黑盒；本地模型和供应链场景再看白盒、训练后门。
4. **用证据等级、来源和代码状态决定复现优先级。** A 级 + 官方开源通常更适合先验证，但仍要核对模型版本和数据集。
5. **结合耗时、设备、专业知识和机会窗口判断可实施性。** 单个载荷成本与完整论文复现成本要分开。
6. **最后看后果、识别难度和防护。** 高影响、高识别难度、低实施门槛的机制应优先进入红队测试和防护基线。
7. **不要直接横向比较不同论文的成功率。** ASR 会受模型版本、判别器、数据集、候选数、查询预算、系统提示和安全策略影响。

## 11. 数据质量提醒

工作簿的“总览”、方法库实际记录和主要统计口径一致为 **55 条：越狱 39、提示注入 9、数据提取 7**。但“口径与评分”页最后一行“质量控制”仍保留旧数字“56 条、40/9/7”，属于未同步的文字残留，后续维护时应改为 55 和 39/9/7。

此外，方法库中有一条写作 `BAP（分类树写作BMP）`，说明分类树和方法库存在简称差异。做自动匹配时应建立别名映射，不要仅用字符串完全相等连接。
